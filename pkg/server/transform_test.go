package server

import "testing"

func TestTransformAlert(t *testing.T) {
	tests := []struct {
		name string
	}{
		{
			name: "transform firing alert",
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {

		})
	}
}
